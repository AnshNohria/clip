# HPC Pipeline Requirements
# Enhanced RS Image Processing Pipeline
#
# synthetic_pipeline.py targets 2x NVIDIA A100 (80GB) natively in bf16:
# no 8-bit quantization and no CPU offload are used, so bitsandbytes and
# segment-anything are no longer required by that pipeline (SAM is only
# still used by zoom_crops_pipeline.py, which is unaffected).

# Core PyTorch (install with CUDA using: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124)
torch
torchvision

# Diffusion models (>=0.30 required for FluxPipeline)
diffusers>=0.30.0
accelerate
safetensors

# Transformers for Qwen2.5-VL, Grounding DINO, etc. (>=4.49 required for
# Qwen2.5-VL support; falls back to Qwen2-VL automatically on older versions)
transformers>=4.49.0
tokenizers
qwen-vl-utils>=0.0.8

# Grounding DINO dependencies
supervision
groundingdino-py

# SAM (Segment Anything Model) - used by zoom_crops_pipeline.py only
segment-anything

# Image processing
Pillow
opencv-python-headless
sentencepiece

# Utilities
tqdm
numpy
scipy
python-dotenv

# HuggingFace Hub
huggingface-hub

# Development tools
ipython

# CLIP models
open-clip-torch